query CharacterList($page: Int, $search: String) {
  characters(page: $page, filter: { name: $search}) {
    info {
      next
    }
    results {
      name
      image
      location {
        name
      }
      status
    }
  }
}
